import { VaalidarCepDirective } from './validar-cep.directive';

describe('VaalidarCepDirective', () => {
  it('should create an instance', () => {
    const directive = new VaalidarCepDirective();
    expect(directive).toBeTruthy();
  });
});
