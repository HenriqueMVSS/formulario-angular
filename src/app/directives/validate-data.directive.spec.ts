import { ValidateDataDirective } from './validate-data.directive';

describe('ValidateDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateDataDirective();
    expect(directive).toBeTruthy();
  });
});
